int suma(int a, int b) {
    int resultado = a + b;
    return resultado;
}

void imprimirSuma(int valor) {
    int x = valor + 1;
    // Esto simula un efecto en el programa
}

int main() {
    int x;
    int y;
    x = 10;
    y = 20;
    
    while(x>4)
    {
        x = x+1;
    }
    

    // Llamada a la función suma
    int z = suma(x, y);

    // Llamada a la función void
    imprimirSuma(z);
    
    return 0;
}
